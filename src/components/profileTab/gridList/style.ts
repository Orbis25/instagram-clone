import { makeStyles, createStyles } from "@material-ui/core";

export const useStyles = makeStyles(() =>
  createStyles({
    images: {
      width: "100%",
      height: "100%",
      cursor: "pointer",
    },
    item: {},
  })
);
