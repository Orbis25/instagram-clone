import { makeStyles, createStyles } from "@material-ui/core";

export const useStyles = makeStyles(() =>
  createStyles({
    iconSave: {
      marginLeft: "auto",
    },
    liked: {
      color: "#ED4956",
    },
  })
);
